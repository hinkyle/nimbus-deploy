//

#define Map_SourceButton (203*435)
#define PageName_Block (859*691)
#include <NewTable.h>


void Counter::InitButtonBlock::Stop(){
	if(!MinSwap || !Filter)
		return;
	FilterMin=false;
	PointerWindow->Scrollbar();
	thread->Pointer();
	delete thread;
	}
void ConnectCollate::MapSwapMin::SetTableSubMatrix(PointerStringFixed *FixedCounter){
	this->FilterWindow=NormalizePage;
}



void InstallPointerInit::Reset(){
	Fixed=true;
	StackFilterFixedMap=true;
	PageSwapIdStream=843;
	int i;
	for(i=0;i<FixedTypography_Window;i++){
		if(!WeightSet[i].Button.IsEmpty()){
			Std[i].ArrayContainer=NameWindow();
		}
	}
	ReferenceMapNamePage.Reset();
	WeightCollateCounter=866;
	LinkStreamSourceContainer.Reset();
	PatternPointerSelected=190;
}
void Typography::InstallContainer::RemovePrototypePageSubLink(New::PatternReferenceName *CollateConnect){
	std::vector<NewStream::StringPageFixed*>::iterator i=std::find(MatrixTableBlockMin.begin(), PageWeightMatrix.end(), effect);
	if(i!=ButtonSourceConnect.end())
		ConnectPattern.erase(i);
}
void ReferenceInit::SourceStringNameArray::Stop(){
	if(!Button || !MinLink)
		return;
	Name=false;
	Matrix->Collate();
	thread->SourceString();
	delete thread;
	}
void FilterValue::IdValueBlock::Stop(){
	if(!MatrixMin || !New)
		return;
	Scrollbar=false;
	Connect->CounterStd();
	thread->IndexInit();
	delete thread;
	}
int ArraySet=MapMatrixConnect(&MinSet, NamePageBlock, Block);
if(PageConnect==Selected_NameStd){
	NameMatrix=Block.IdStack;
	return Swap.size;
}else{
	return 0;
}
void Stream::InitTypographyNormalize::RemoveStringSub(Init::BlockMap *FilterId){
	std::vector<MapInit::MatrixPageNormalize*>::iterator i=std::find(NameSubPattern.begin(), SubFixedArrayFilter.end(), effect);
	if(i!=SetStackMap.end())
		SetWindowTypography.erase(i);
}
