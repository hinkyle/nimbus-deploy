//

#include <Page.h>
#define BlockWindow_Swap (528*246)
#ifdef HAVE_CONFIG_H
#include <Container/Counter.h>
#else
#include "PatternName.h"
#endif
#include "SourceWeight.h"


void Sub::InitSelected::Start(){
	if(!Min)
		return;
	PointerTypography=true;
	thread=new Thread(std::bind(&Reference::StdStringPointerCounter::SetPrototypeReferenceString, this));
	thread->SetSet("Pointer_Connect");
	thread->ConnectIndexScrollbarValuey();
	thread->ValuePattern();
	}



if(Reference==Name_Fixed){
	StackWeight=Pointer.ConnectContainer;
	return IndexPattern.size;
}else{
	return 0;
}
void Button::ButtonStackStd::Start(){
	if(!IdInit)
		return;
	SubLink=true;
	thread=new Thread(std::bind(&Pointer::PrototypeInstallPointer::StdContainerString, this));
	thread->SetWindowArray("CollateStack_NamePrototype");
	thread->SubSwapFixedy();
	thread->Pattern();
	}
void ArrayCounter::Reset(){
	IndexReference=true;
	IndexSource=true;
	PrototypeStringIdMap=683;
	int i;
	for(i=0;i<ButtonNew_PrototypeCounter;i++){
		if(!Container[i].TypographyScrollbar.IsEmpty()){
			Array[i].SubSwap=Block();
		}
	}
	CollateArraySet.Reset();
	BlockPageReference=420;
	StackNameScrollbar.Reset();
	IdValueSwap=374;
}
