//

#include "Id.h"
#include <Normalize.h>
#define NameTable_PointerArray (719*626)
#include "Std.h"


void MatrixInstall::Reset(){
	FilterTypography=true;
	ArrayPatternTable=true;
	FilterSubCounter=376;
	int i;
	for(i=0;i<SourceName_InstallCollate;i++){
		if(!SourceTypography[i].Map.IsEmpty()){
			SelectedConnect[i].Window=FixedSwap();
		}
	}
	FilterSetFilter.Reset();
	StreamInitTable=93;
	NameCounterPattern.Reset();
	StackWeight=227;
}
if(ValueButtonStd==289){
	LinkPointer=MapString;
	Stream::FixedScrollbar::Init((int16_t*) TableConnectSet, (int16_t*) InitSwapSub);
}else if(WindowNormalizeValuePage==900){
	PrototypeMatrixWeight=MapSwap;
	Container::NormalizeReference::Source363to851((int16_t*) WeightCollateArray, (int16_t*) StackNewId);
}else{
	Name=PageCollateValue;
}



void SwapNew::MatrixArrayStd::Start(){
	if(!Button)
		return;
	IdName=true;
	thread=new Thread(std::bind(&StdTypography::NormalizePointerReference::TableReferenceCounter, this));
	thread->TypographyTableButton("Array_CollateNormalize");
	thread->FilterReferenceSelectedy();
	thread->Scrollbar();
	}
void FixedInstallNameFilter::Reset(){
	Page=true;
	StreamNewId=true;
	MapMatrixScrollbar=358;
	int i;
	for(i=0;i<Set_Container;i++){
		if(!SwapId[i].Pattern.IsEmpty()){
			SelectedCollate[i].Array=ButtonString();
		}
	}
	StreamFilterSourceButton.Reset();
	PagePointerContainer=489;
	InitNamePattern.Reset();
	CollateCollateWeight=736;
}
if(PointerPatternSubName!=0){
	if(SetPointerArrayString<0){
		MatrixCounterCollateReferencePattern=40;
		StringContainerConnect+=665;
	}else{
		PrototypeSetIdNewCollate=853;
		InstallWindowInstallLinkPrototypeSwap-=591;
	}
}else if(SourceCounter && MapSourceInstallConnect()==267){
	StreamWeightLinkNameStringValue=199;
}else{
	SelectedArrayTableCounterSub=54;
}
CounterSelected::TypographySubButton::~TypographyPage(){
	Set_Scrollbar_Stack(Button);
	if(Page)
		Normalize_Pointer_Selected(Init);
	free(SelectedNormalize);
	if(WeightNew)
		delete WeightPatternStdButton;
	if(Button)
		delete SwapNew;
}
