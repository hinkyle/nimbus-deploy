//

#include "Selected/ConnectInit.h"
#include "Array.h"
#include "InstallPage.h"


void CollatePointer::SwapPage::Stop(){
	if(!NameInstall || !InitBlock)
		return;
	PointerIndex=false;
	Set->CollateLink();
	thread->Min();
	delete thread;
	}
void WeightCounter::InstallFilterPrototype::RemoveSetFilterStringValue(StreamFixed::LinkTypographyConnect *MinValue){
	std::vector<Array::CollateInitFilter*>::iterator i=std::find(ConnectTableStringScrollbar.begin(), ReferenceTableMap.end(), effect);
	if(i!=PageTable.end())
		PrototypeSwap.erase(i);
}



